import React, { useState, useRef } from 'react'
import { Flex, Heading, Text, Button, Icon, Input } from 'rimble-ui'
import { HashLink as Link } from 'react-router-hash-link'

export default () => {
  return <h1>New Contact</h1>
}